# -*- python -*-
# Copyright (c) 2011 The Native Client Authors. All rights reserved.
# Use of this source code is governed by a BSD-style license that can be
# found in the LICENSE file.

Import('env')

env.ComponentProgram('native_worker', 'native_worker.c',
                     EXTRA_LIBS=['srpc',
                                 'platform',
                                 'gio',
                                 'imc',
                                 'imc_syscalls',
                                 'pthread'])

# Note that the html is required to run this program.
env.Publish('native_worker', 'run',
            ['native_worker.html'])

env.ComponentProgram('mandel_worker', 'mandel_worker.c',
                     EXTRA_LIBS=['srpc',
                                 'platform',
                                 'gio',
                                 'imc',
                                 'imc_syscalls',
                                 'pthread'])

env.Publish('mandel_worker', 'run', ['mandel_worker.html',
                                     'mandel_worker.js'])
