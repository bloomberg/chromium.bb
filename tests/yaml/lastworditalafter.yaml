# Converted from lastworditalafter.c

# First check italics with the English table
table: [tables/en-us-g2.ctb]
tests:
  - - "He said it wasn't always working as expected."
    - ".,he sd x wasn't .alw .\"w+ z expect$4"
    - typeform:
        italic: '++                ++++++++++++++             '
      xfail: true
    # actual output: ".',he sd x wasn't .'alw .'\"w+ z expect$4"

# Then check a test table that defines lastworditalafter
table: [tables/de-ch-g1.ctb]
tests:
  - - "Er sagte es funktioniere nicht immer wie erwartet."
    - "_ER SAGTE ES __FUNKTION0RE NI4T', IMMER W0 ERWARTET."
    - typeform:
        italic: '++          ++++++++++++++++++                    '
      xfail: true
    # Actual output: "ER'. SAGTE ES FUNKTION0RE NI4T'. IMMER W0 ERWARTET."
