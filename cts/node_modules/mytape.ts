import tape from 'tape';
import { ParamIterable } from 'framework';

export default test;
function test(name: string, cb: tape.TestCase) {
  tape(name, async t => {
    await cb(t);
    t.end();
  });
}

// TODO: test.skip
// TODO: take params
// TODO: handle exceptions?
