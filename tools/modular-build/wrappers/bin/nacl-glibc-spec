
# TODO(mseaborn): We could remove the need for this spec file if we
# change nacl-gcc's default one with an option to omit "-lnacl".

*lib:
-lc -lgcc

# In principle, this also ought to work:
# + %<-lnacl
