# These are the build arguments used for a 64-bit Linux build:
# Make sure you run 'gclient runhooks' with 
#   GYP_DEFINES="branding=Chrome buildtype=Official" 
# to pull in the sysroots.
symbol_level=0 is_official_build=true is_debug=false is_chrome_branded=true cpu_arch="x64"
