include_rules = [
  "+net",
  "+third_party/libwebp/webp",
  "+ui",
]

specific_include_rules = {
  ".*\.cc$": [
    # Allow browser_thread.h until a web/ replacement is built out. Note that
    # headers in public/ are not subject to this exception.
    "+content/public/browser/browser_thread.h",
  ],
}
