# This suite runs the tests in LayoutTests/events/eventlisteneroptions with --stable-release-mode, with which
# we run tests with only stable feature (no experiments).
# See the virtual_test_suites() method in Tools/Scripts/webkitpy/layout_tests/port/base.py.
