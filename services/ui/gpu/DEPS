include_rules = [
  "+components/viz",
  "+gpu/command_buffer",
  "+gpu/config",
  "+gpu/ipc",
  "+gpu/ipc/common",
  "+gpu/ipc/service",
  "-services/ui/common",
  "+services/viz/privileged/interfaces/gl",
]

specific_include_rules = {
  "gpu_main.*": [
    "+services/ui/surfaces",
  ]
}
