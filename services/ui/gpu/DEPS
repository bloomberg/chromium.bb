include_rules = [
  "+components/viz",
  "+gpu/command_buffer",
  "+gpu/config",
  "+gpu/ipc",
  "+gpu/ipc/common",
  "+gpu/ipc/service",
  "+media/gpu",
  "+media/mojo/services",
  "-services/ui/common",
]

specific_include_rules = {
  "gpu_main.*": [
    "+services/ui/surfaces",
  ]
}
