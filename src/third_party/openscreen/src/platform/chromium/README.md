# Chromium Platform

The code in this directory implements the platform classes/functions for when
the Open Screen library is being embedded within a Chromium build. There are
simple adapters to Chromium-provided infrastructure.
