#include <CoreServices/CoreServices.r>

resource 'STR#' (126) { {
  "NativeClient",
  "NativeClient Plug-in"
} };

resource 'STR#' (127) { {
  "NativeClient Module - NPAPI bridge",
  "NativeClient Module - SRPC",
} };

resource 'STR#' (128) { {
  "application/x-nacl-npapi",
  "nexe",
  "application/x-nacl-srpc",
  "nexe",
} };
