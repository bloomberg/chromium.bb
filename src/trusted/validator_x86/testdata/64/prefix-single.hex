# lock add %eax,(%rsp)
F0 01 04 24
# mov %edi,%edi
# lea (%r15,%rdi),%rdi
# rep stos %eax,%es:(%rdi)
89 FF
49 8D 3C 3F
F3 AB
# mov %edi,%edi
# lea (%r15,%rdi),%rdi
# repne scas %es:(%rdi),%al
89 FF
49 8D 3C 3F
F2 AE
# fs mov %eax,%eax
64 89 C0
# gs mov %eax,%eax
65 89 C0
# mov %ax, %ax
66 89 C0
# rex jmp $
40 EB FD

# Forbidden prefixes.
# rep push %eax
F3 50
# mov %edi,%edi
# lea (%r15,%rdi),%rdi
# repne stos %eax,%es:(%rdi)
89 FF
49 8D 3C 3F
F2 AB
# mov %eax,%cs:(%rsp)
2E 89 04 24
# mov %eax,%ss:(%rsp)
36 89 04 24
# mov %eax,%ds:(%rsp)
3E 89 04 24
# mov %eax,%es:(%rsp)
26 89 04 24
# mov %eax,%fs:(%rsp)
64 89 04 24
# mov %eax,%gs:(%rsp)
65 89 04 24
# mov %eax,(%esp)
67 89 04 24
# cs jmp $
2E EB FD
# ss jmp $
36 EB FD
# ds jmp $
3E EB FD
# es jmp $
26 EB FD
# jmp dword $
66 EB FD

# Mandatory prefix.
# phaddw %xmm0,%xmm1
66 0f 38 01 c8
# addsd %xmm0,%xmm1
f2 0f 58 c8
# cmpeqss %xmm0,%xmm1
f3 0f c2 c8 00
