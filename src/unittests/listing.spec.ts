export const description = `
Unit tests for test filter queries.
`;

import { DefaultFixture, TestGroup } from '../framework/index.js';

export const group = new TestGroup();

group.test('basic', DefaultFixture, () => {
});
