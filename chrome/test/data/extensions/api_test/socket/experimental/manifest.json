{
  "name": "chrome.socket",
  "version": "0.1",
  "description": "browser test for chrome.socket API to make sure experimental permissions are enforced",
  "app": {
    "background": {
      "scripts": ["background.js"]
    }
  },
  "permissions": [
    {"socket": [
      "tcp-connect",
      "tcp-listen",
      "udp-send-to",
      "udp-bind"
    ]}
  ]
}
